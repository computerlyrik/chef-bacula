# 
# This file holds defaults for new Bacula packages installed on this system.
# It is used only when you install a new Bacula package, and can be
# safely removed at any time.

DIRPASSWD=<%= node['bacula']['dir']['password'] %>
DIRMPASSWD=<%= node['bacula']['dir']['password_monitor'] %>
SDPASSWD=<%= node['bacula']['sd']['password'] %>
SDMPASSWD=<%= node['bacula']['sd']['password_monitor'] %>
FDPASSWD=<%= node['bacula']['fd']['password'] %>
FDMPASSWD=<%= node['bacula']['fd']['password_monitor'] %>
